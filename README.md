# Team 6

## PDF extraction (Deliverable #2)

- `pdf_parser.py` — `extractTextFromPdf` using PyPDF2
- `test_pdf_parser.py` — pytest (valid path, invalid path, empty path)
- `sample.pdf` — small PDF used by the tests

Install: `python -m pip install -r requirements.txt`  
Run tests: `python -m pytest test_pdf_parser.py -v`